#include "main.h"

/**
 * add : function that adds two integers and returns the result.
 * @n: first integer to be added to m.
 * @m: second integer to be added to n.
 * Return: sum of n and m.
 */
int add(int n, int m)
{
	int sum;

	sum = n + m;

	return (sum);
}

